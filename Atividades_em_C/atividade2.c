#include <stdio.h>
#include <stdlib.h>

int main()
{
    // float c, f;

    // printf("Digite um numero: ");
    // scanf("%f", &f); 

    // c = (f-32.0) * (5.0 / 9.0);

    // printf("\no resulta em celcio: %f\n", c);

    int c, f;

    printf("Digite um numero: ");
    scanf("%d", &f); 

    c = (f-32.0) * (5.0 / 9.0);

    printf("\no resulta em celcio: %d\n", c);

    return 0;


}